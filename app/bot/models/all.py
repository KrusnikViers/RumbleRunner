# Make sure, that all custom models are imported to this file. This will allow migration & ORM engine to detect them.
from app.bot.models.game_session import GameSession
from app.bot.models.participation import Participation
from app.bot.models.player import Player
